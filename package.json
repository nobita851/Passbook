{
  "name": "Passbook History",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ nobita851/Passbook History",
    "create-local": "graph create --node http://localhost:8020/ nobita851/Passbook History",
    "remove-local": "graph remove --node http://localhost:8020/ nobita851/Passbook History",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nobita851/Passbook History"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
